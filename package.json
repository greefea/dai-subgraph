{
  "name": "dai-ethereum",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "npx graph codegen",
    "build": "graph build",
    "deploy": "npx graph deploy --product hosted-service wyaming89/subgraph004",
    "create-local": "graph create --node http://localhost:8020/ dai-ethereum",
    "remove-local": "graph remove --node http://localhost:8020/ dai-ethereum",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dai-ethereum",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0",
    "prettier": "2.7.1"
  }
}
