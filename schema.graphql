type token @entity {
  id: ID!
  symbol: String!
  supply: BigInt!
  decimals: Int!
}

type transfer @entity {
    id: ID!
    from: String!
    to: String!
    amount: BigInt!
}

type dailyTransfer @entity {
    id: ID!
    transfers:Int!
    amount:BigInt!
}
